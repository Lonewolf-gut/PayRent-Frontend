"use client";

/** Subscription trials are no longer offered for merchants or affiliates. */
export function TrialStatusBanner(_props?: {
  className?: string;
  fullWidth?: boolean;
}) {
  return null;
}
